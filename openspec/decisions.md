# 架构决策日志

**日期 + 决策 + 理由** 格式记录重大架构决策。

2026-08-02 — 反转 b245f28：重新采用 cookie-* 命名（取代 restclient-cookie-*）
理由：项目 Elisp 包收敛为单一包（elisp/cookie.el）后无命名空间冲突实际场景；
      用户偏好简短前缀 cookie-*；此决策以日志显式记录反转，而非静默违背
      2026-04-03 的 b245f28 提交（"避免与通用 cookie 命名冲突"）。
